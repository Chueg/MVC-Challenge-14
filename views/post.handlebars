<div>
<div class="smokem"  data-index="{{id}}">
  <div>
  <h2>{{id}}.   {{name}}   <p>Posted by {{user.name}} on {{format_date date_created}}</p> </h2>
  </div>
  <p>
    {{description}}
  </p>
</div>

<section class="fun">
{{#if logged_in}}
  <h2>Leave a comment!</h2>
        <form class="form cool-form">
        <label for="comment">Comment:</label>
        <input class="form-input smump" type="text" id="Comment" />
        <button class="btn btn-primary " type="submit">Submit</button>
      </form>
      
        {{else}}
        <h2>Login to leave a comment!</h2>
        <a href="/login">login</a>
        {{/if}}

</section>
{{#each commentData as |comment|}}


  <div>
    <div class = "cream">
  <div>
  <h2><p>Posted by {{comment.name}} on {{format_date comment.date_created}}</p> </h2>
  </div>
  <p>
    {{comment.bomment}}
  </p>

        {{/each}}
</div>


<script src="/js/commentpost.js"></script>



